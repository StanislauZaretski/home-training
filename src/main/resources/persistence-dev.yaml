spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: qwe
    password: qwe
  jpa:
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always
application:
  name: domen-dev
server:
  port: 8080
  error:
    whitelabel:
      enabled: false